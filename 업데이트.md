# 업데이트

## 2026-05-04 - BG-6 SettingsWindow 리네이밍 + region 콜백 통합 (Phase 0 분리 사이클 종결)

목표: BG-1~BG-5 사이클로 책임이 분리됐으니 클래스 이름을 실제 책임에 맞게 정리하고,
`CaptureRegionController`의 owner 위젯 의존을 콜백으로 통합한다 (ARCHITECTURE.md §3 BG-6 계획).

수정:
- `TransparentWindow` → `SettingsWindow` 일괄 리네이밍 (코드/테스트/pre-release/현재-상태 문서).
  `Cocktail완성본.py`, `smoke_tests.py`, `pre_release_check.py`, `ARCHITECTURE.md`,
  `TECH_STACK.md`, `COMMERCIAL_SOTA_REVIEW.md`.
- `CaptureRegionController.__init__(owner)` → `__init__(is_self_hwnd)`.
  `self.owner` 속성 제거. `BackgroundController`와 같은 `is_self_hwnd` 콜백을 공유.
- `CaptureRegionController.foreground_window_rect`: `self.owner.winId()` /
  `getattr(self.owner, "overlay", ...)` 분기 제거 → `self._is_self_hwnd(int(hwnd))` 한 줄.
- `SettingsWindow.paintEvent` 빈 override 제거 — Qt 기본이 위젯 트리를 자동 처리.
- `_on_state_ready`에서 `self.update()` 호출 제거 — settings는 그릴 게 없으므로 overlay만 갱신.
- 코멘트 잔재 정리: "ControlWindow(=TransparentWindow)" 같은 역사적 표기를 BG-6 시점 표현으로 갱신.
- BackgroundController docstring 갱신: BG-4/5/6 분리 사이클 누적 기록.
- 5초 체크리스트에 BG-6 항목 5개 추가 (rename, region callback, dead paintEvent, ARCHITECTURE 동기화).

테스트 보강:
- `smoke_tests.py`: `test_bg6_settings_rename_and_region_callback` 신규.
  기존 `test_overlay_hwnd_guards`/`test_capture_region_controller` BG-6 변경에 맞춰 갱신.
- `pre_release_check.py`: `check_bg6_rename_and_region_callback` 신규.

박사 자체 비판 리뷰:
- 리네이밍 시 smoke/check 검사 문자열까지 모두 동기화 (replace_all 후 broken assertion 2건 즉시 수정).
- region.owner 통합으로 `region`/`controller` 둘 다 동일 콜백 사용 — 자기 hwnd 가드 정책 일원화.
- `SettingsWindow` 빈 paintEvent 제거가 모드별 그리기에 영향 없음 확인 (OverlayWindow만 그림 담당).
- ARCHITECTURE.md "Phase 0 분리 사이클 종결" 명시. 다음 단계는 RegionEditor 또는 Phase 1+.

검증:
- `python -m py_compile "Cocktail완성본.py" smoke_tests.py pre_release_check.py` 통과.
- `python smoke_tests.py` 통과 (21 fast tests).
- `python pre_release_check.py --allow-build-artifacts` 통과 (14 OK).
- `python "Cocktail완성본.py" --self-test` 통과.

## 2026-05-04 - BG-5 red_rect 좌표계 통일

목표: `red_rect`를 화면 절대 좌표로 전환해 settings window 이동/숨김에 대한 의존을 제거하고, 워커/오버레이의 좌표 변환 코드를 단순화한다 (ARCHITECTURE.md §4 결정).

수정:
- `CaptureRegionController.__init__`: 기본 red_rect를 위젯 좌표 `QRect(px(100), px(75), px(600), px(375))` →
  절대 좌표 `QRect(240, 215, 600, 375)`로 전환. 종전 사용자가 보던 영역과 연속성 유지.
- `CaptureRegionController.update_for_mode`: `owner.geometry()` 의존 제거.
  `MODE_WINDOW`/`MODE_HOVER`에서 활성 창/마우스 절대 좌표를 그대로 `red_rect`에 저장.
- `BackgroundController.capture_and_translate_async`: `region.owner.geometry()` 변환 4줄 제거.
  `x1 = int(rr.x())` 같은 단순 직접 사용으로 교체.
- `OverlayWindow.paintEvent` MODE_BOX 박스: `ctl.geometry().x()` / `cy + rect.y()` 변환 제거.
  단순히 절대 → overlay-내 변환 (`rect.x() - wx`)만 수행.
- `TransparentWindow.initUI`: size_grip 위치 계산을 위해 위젯 좌표를 가정하던 `setGeometry`
  코드 제거. grip은 여전히 hide 상태 (BG-1 재발 방지).
- `smoke_tests.py`: `test_red_rect_absolute_coordinates` 추가.
  `_method` 헬퍼가 `AsyncFunctionDef`도 인식하도록 보강.
- `pre_release_check.py`: `check_red_rect_absolute` 추가.
- `errors.md` 5초 체크리스트에 BG-5 항목 3개 추가 (red_rect 절대, OverlayWindow 의존 없음, AST 헬퍼).

박사 자체 비판 리뷰:
- 결함 1건 즉시 수정: smoke 테스트가 `capture_and_translate_async`를 찾지 못함.
  Python AST는 `def`(FunctionDef)와 `async def`(AsyncFunctionDef)가 분리된 노드 타입.
  `_method` 헬퍼를 `(FunctionDef, AsyncFunctionDef)` 둘 다 받도록 수정.
- MODE_BOX UX 변화: settings window 이동이 더 이상 빨간 박스를 끌고 가지 않음.
  사용자 명시 절대 영역 의도에 부합 (UX-1 기본이 MODE_WINDOW이므로 영향 작음).
  영구 영역 편집 UX는 BG-6+ `RegionEditor` 사이클에서 보강 예정.

검증:
- `python -m py_compile "Cocktail완성본.py" smoke_tests.py pre_release_check.py` 통과.
- `python smoke_tests.py` 통과 (20 fast tests).
- `python pre_release_check.py --allow-build-artifacts` 통과 (13 OK).
- `python "Cocktail완성본.py" --self-test` 통과.

## 2026-05-04 - BG-4 BackgroundController 분리

목표: `TransparentWindow`가 보유하던 워커/OCR/UIA/모델 lifecycle/캐시 타이머를 분리해, 설정 창은 UI 책임만 갖게 한다.

수정:
- `BackgroundController(QObject)` 신설.
- 5개 엔진 시그널 이동: `state_ready`, `failure_alert`, `model_ready`, `progress_msg`, `status_msg`.
- worker thread + asyncio loop(`run_asyncio_loop`, `capture_and_translate_async`) 이동.
- OCR/SmartOCR(`perform_ocr_with_boxes`, `_ocr_langs`, `_smart_lang_for_image`, `_detect_script_via_osd`,
  `_lines_from_tess`, `_src_hint`, `_ocr_backend_name`) 이동.
- UIA 라우팅(`_uia_collect_and_translate`)과 민감 창 가드(`_check_sensitive_pause`) 이동.
- 모델 lifecycle(`_preload_model`)과 영구 캐시 타이머(`_on_cache_save_tick`) 이동.
- 자동 일시정지 플래그(`_capturable_paused`, `_sensitive_paused`)와 SmartOCR 캐시(`_script_cache`) 이동.
- 환경 진단(`env_status`, `env_problems`) 이동.
- `RuntimeOptions(@dataclass(frozen=True))` 신설 — 워커가 매 iteration 콤보 상태를 immutable 스냅샷으로 읽음.
- `TransparentWindow`는 `BackgroundController`를 `__init__`에서 생성하고
  `region`/`options_provider`/`is_self_hwnd` 3개만 의존 주입.
- TransparentWindow에 `_snapshot_options`, `_is_self_hwnd` helper 추가.
- backward-compat proxy property(`translated_text`, `model_loaded`, `capturable`) 유지로
  `OverlayWindow.paintEvent`/closeEvent 회귀 위험 0.
- `_apply_state` → `_on_state_ready`로 단순화: controller가 `translated_text`를 자체 슬롯으로 갱신하므로
  settings/overlay는 redraw만 트리거.
- closeEvent 단순화: `controller.stop()` 한 호출로 stop_thread + thread join + cache save 처리.
- `smoke_tests.py` `test_background_controller_owns_engine` 추가 + 기존 테스트 5건 갱신.
- `pre_release_check.py` `check_background_controller` 추가.
- `errors.md` 5초 체크리스트에 BG-4 항목 5개 추가.

박사 자체 비판 리뷰:
- forward-ref 결함 1건 즉시 수정. `_snapshot_options`의 `-> RuntimeOptions` 어노테이션이 TransparentWindow 클래스
  정의 시점(line 1050)에 평가되는데 RuntimeOptions는 line 2043에서 정의됨 → NameError 발생.
  문자열 어노테이션 `-> "RuntimeOptions"`로 forward-ref 처리.
- 남은 부채: BackgroundController 워커 안에서 `self.region.owner.geometry()`로 settings 창 좌표 의존.
  BG-5에서 `red_rect`를 절대 좌표로 통일하면 이 의존 제거 가능.
- 다음 단계 BG-5(좌표계 통일) → BG-6(SettingsWindow 리네이밍 + OverlayRenderer 정리) 순서.

검증:
- `python -m py_compile "Cocktail완성본.py" smoke_tests.py pre_release_check.py` 통과.
- `python smoke_tests.py` 통과 (19 fast tests).
- `python pre_release_check.py --allow-build-artifacts` 통과 (12 OK).
- `python "Cocktail완성본.py" --self-test` 통과.

## 2026-05-04 16:14:02 +09:00 - BG-3 CaptureRegionController 분리

목표: settings panel이 캡처 영역 상태까지 직접 소유하는 구조를 줄이고, 추후 전용 `RegionEditor`를 붙일 수 있는 중간 계층을 만든다.

수정:
- `CaptureRegionController` 추가.
- 캡처 모드, hover 영역 크기, `red_rect` 상태를 `CaptureRegionController`로 이동.
- 활성 창 좌표 계산, 마우스 hover 좌표 계산, 모드별 캡처 영역 갱신을 `CaptureRegionController`가 담당.
- `TransparentWindow`에는 `capture_mode`, `hover_size`, `red_rect` proxy property를 남겨 기존 워커/오버레이 호출부의 회귀 위험을 줄임.
- settings panel의 `initUI()`에서 `red_rect`를 직접 초기화하지 않도록 수정.
- `smoke_tests.py`에 `test_capture_region_controller` 추가.
- `pre_release_check.py`에 `check_capture_region_controller` 추가.
- `TECH_STACK.md`, `COMMERCIAL_SOTA_REVIEW.md`에 BG-3 구조를 기록.

비판적 리뷰:
- 이번 단계는 상태 소유권 분리다. 아직 실제 영역 편집 UI는 없다.
- 다음 단계에서 `RegionEditor`를 만들 때는 `CaptureRegionController.red_rect`만 조작하게 해야 하며, settings panel resize는 계속 캡처 영역을 바꾸면 안 된다.

검증:
- `python -m py_compile "Cocktail완성본.py" smoke_tests.py pre_release_check.py` 통과.
- `python smoke_tests.py` 통과.
- `python pre_release_check.py --allow-build-artifacts` 통과.
- `python "Cocktail완성본.py" --self-test` 통과.
- `python -m PyInstaller --noconfirm build.spec` 통과.
- `dist\Cocktail\Cocktail.exe --self-test` exit code 0.
- 배포 산출물: 786.38MB / 6372 files.
- 기본 배포에서 `paddle`, `paddleocr`, `modelscope` 제외 확인.
- PyTorch 개발 산출물 `.lib`/`torch/include` 제외 확인.
- `tessdata`: `ara`, `chi_sim`, `chi_tra`, `deu`, `eng`, `fra`, `jpn`, `kor`, `osd`, `rus` 포함 확인.

## 2026-05-04 15:48:52 +09:00 - BG-2 앱 생명주기 컨트롤러 + 상용/SOTA 기준선

목표: 백그라운드 번역기 배포 구조에서 `__main__`에 실행 생명주기가 흩어지는 결함을 줄이고, SOTA/상용 라이선스 판단 기준을 문서화한다.

수정:
- `CocktailAppController` 추가.
- `QApplication`, `setQuitOnLastWindowClosed(False)`, `TransparentWindow`, `OverlayWindow` 생성/연결을 컨트롤러 한 곳으로 이동.
- `main(argv=None)` 추가. `--self-test`는 QApplication 생성 전에 끝나고, 일반 실행만 컨트롤러를 시작한다.
- `smoke_tests.py`에 `test_app_lifecycle_controller` 추가.
- `pre_release_check.py`에 `check_app_lifecycle_controller` 추가.
- `COMMERCIAL_SOTA_REVIEW.md` 신규 작성. 공식 근거 기준으로 Tesseract/tessdata/PaddleOCR/PySide6/Transformers/PyTorch/모델 정책을 정리.
- `TECH_STACK.md`, `LICENSE-3RDPARTY.md`, `README.md`, `build.spec`를 새 기준선과 동기화.

검증:
- `python -m py_compile "Cocktail완성본.py" smoke_tests.py pre_release_check.py` 통과.
- `python smoke_tests.py` 통과.
- `python pre_release_check.py --allow-build-artifacts` 통과.
- `python "Cocktail완성본.py" --self-test` 통과.
- `python -m PyInstaller --noconfirm build.spec` 통과.
- `dist\Cocktail\Cocktail.exe --self-test` exit code 0.
- 배포 산출물: 786.37MB / 6372 files.
- 기본 배포에서 `paddle`, `paddleocr`, `modelscope` 제외 확인.
- `tessdata`: `ara`, `chi_sim`, `chi_tra`, `deu`, `eng`, `fra`, `jpn`, `kor`, `osd`, `rus` 포함 확인.
- PyTorch 개발 산출물 `.lib`/`torch/include` 제외 확인.
- `COMMERCIAL_SOTA_REVIEW.md`, `업데이트.md`, `errors.md`가 dist에 포함되는 것 확인.

비판적 리뷰:
- 현재 구조는 이전보다 안전하지만, `TransparentWindow`가 아직 설정 UI, 워커 루프, 트레이, OCR/번역 상태를 많이 보유한다.
- 다음 구조 부채는 `BackgroundController`, `SettingsWindow`, `RegionEditor`, `OverlayRenderer` 분리다.
- Hugging Face 모델은 라이브러리 라이선스와 모델 라이선스가 다르므로 모델별 카드 확인 없이는 추가하지 않는다.

## 2026-05-04 15:32:09 +09:00 - BG-1 Tray-first 백그라운드 전환

목표: 투명 컨트롤바가 상시 떠 있는 개발용 오버레이 구조를 줄이고, 실제 백그라운드 번역기처럼 설정창은 최초 1회 또는 트레이/단축키에서만 열리게 한다.

수정:
- `TransparentWindow`를 상시 투명 컨트롤바가 아니라 `Cocktail Settings` 설정 패널로 전환.
- 시작 시 `QSettings("Cocktail", "OverlayTranslator")`의 `ui/first_run_complete` 값을 확인.
- 최초 실행이면 설정창을 표시하고, 이후 실행부터는 트레이로 숨은 상태에서 백그라운드 동작.
- 설정창의 `확인` 또는 창 닫기 동작 시 `ui/first_run_complete=True` 저장.
- `QApplication.setQuitOnLastWindowClosed(False)` 적용으로 설정창을 닫아도 앱/트레이/번역 루프가 종료되지 않게 변경.
- 트레이 `열기` 및 `Ctrl+Shift+A`는 설정창을 다시 여는 진입점으로 유지.
- 설정창 표시 시 ControlWindow/OverlayWindow capture affinity 상태를 다시 맞춤.
- 자체 리뷰 보강: 설정창 크기 변경이 기존 `red_rect` 캡처 영역을 음수/비정상 값으로 바꾸지 않도록 `resizeEvent`에서 red box 크기 변경을 제거.
- 기존 `QSizeGrip`은 설정창에서 숨김. 영역 크기 조정은 추후 별도 `RegionEditor`가 담당해야 함.

비판적 리뷰로 추가한 재발 방지:
- `smoke_tests.py`에 `test_tray_first_settings_window` 추가.
- `pre_release_check.py`에 `check_tray_first_background` 추가.
- 검증 기준: QSettings, 최초 1회 설정, 무조건 `self.show()` 금지, 일반 설정창 플래그, 창 닫아도 앱 미종료.
- 추가 검증 기준: 설정창 resize가 캡처 영역을 변경하지 않을 것, settings panel에 resize grip을 노출하지 않을 것.

한계:
- 이번 단계는 안전한 1차 전환이다. Worker/Overlay 소유권은 기존 `TransparentWindow`에 유지했다.
- 다음 구조 개선 후보는 `BackgroundController`, `SettingsWindow`, `OverlayRenderer`, `RegionEditor` 클래스로 완전 분리하는 것이다.
