# Cocktail — 다음 세션 인수인계

작성 2026-09-05, 갱신 2026-09-07. 이 문서 하나만 읽고 이어서 진행할 수 있게 쓴다.
**갱신 규칙**: 단계를 끝낼 때마다 "현재 위치"와 "합격선 점수표"를 갱신하고, 다음 세션이 또 처음부터 조사하지 않게 한다.

---

## 0. 이 프로젝트가 무엇이고 어디로 가는가

`C:\Users\ui2030\Documents\capstone_translate-main` — "Cocktail" 화면 번역기.
화면 캡처 → 문자 인식(OCR) → 로컬 번역 → 원문 위에 투명 오버레이로 자막.

**목표: 상용 지향.** 개인/지인 배포도 병행. 그래서 **라이선스가 설계를 지배한다**(§4).

git repo 아님 — GitHub 반영은 별도 clone 경유(github.com/ui2030/capstone_translate main).

## 1. 실행 환경 (틀리면 아무것도 안 돌아간다)

- 파이썬: **반드시** `C:\Users\ui2030\anaconda3\envs\cd\python.exe` (3.11, torch 2.11 CUDA, transformers 5.8.1). 시스템 python 3.13엔 패키지 없음
- Tesseract: `C:\tesseract\tesseract.exe`. 시스템 tessdata엔 eng/osd만, **프로젝트 `tessdata/`에 chi_sim·chi_tra·jpn·kor 등 10종**
- 개발 PC: 주 1920×1080@100% + 보조 3840×2160@175%(-3840,-535), 가상 데스크톱 5760×2160, RTX 3060 12GB

검증 4종:
```
python -m py_compile cocktail*.py
python test_cocktail.py            # --full 이면 모델 통합 테스트 포함
python pre_release_check.py --allow-build-artifacts
python cocktail.py --self-test
```

## 2. 코드 구조

7모듈, 의존 방향 아래→위 단방향:
`cocktail.py`(엔트리) / `cocktail_platform.py`(win32·DPAPI·자동실행) / `cocktail_capture.py`(영역·프레임해시) / `cocktail_ocr.py` / `cocktail_translate.py` / `cocktail_engine.py`(워커) / `cocktail_ui.py`.
단일 파일 `Cocktail완성본.py`는 **없어졌다**(2026-08-03 분할). 역사 문서에 남은 옛 파일명 참조는 의도적 보존.

`errors.md` 맨 앞 **"0. 코드를 만지기 전 5초 체크리스트"를 먼저 읽을 것.**

## 3. 합격선 — "완성"의 정의

사용자가 정한 6개. **이 표를 채우는 것이 프로젝트의 끝이다.**

**판정은 `python bench/score.py` 로만 낸다** (0단계에서 만든 자동 채점판, `bench/README.md`).
아래는 **2026-09-07 폰트 패스 이후** 채점판 실측이다(기준 5는 2026-09-05 값 그대로). 눈으로 본 인상은 이 표에 못 들어온다.

⚠ **기준 4는 2026-09-07에 정의가 바뀌었다** — 예전 숫자(p50 1.05s)는 *한 사이클 시간*이라 지금 숫자와 비교하면 안 된다(errors.md BM-1).

| # | 기준 | 합격 조건 | 채점판 실측 | 상태 |
|---|---|---|---|---|
| 1 | 작은 글씨 | 14px 이하 시료 글자 오류율 ≤5% | CER 최대 **2.8%** (23시료 평균 **0.22%**. 폰트 축 시료 추가 직후엔 **100%**였다 — errors.md OU-2/OU-3) | **통과** (2026-09-07 폰트 패스) |
| 2 | 오번역 없음 | 치명적 오역 0건 + 정상문장 숨김 오탐 ≤1% | 오탐 **0/79 = 0.0%** · 확정 오역 **0건** (대리지표 의심 3건은 사람 확인 대상) | **통과** |
| 3 | 다중 문맥 화면 | 문단 경계 오류 화면당 ≤1건 | doc **0건** · fullhd **0건** · multi_context **0건** (참고: UI 화면 8건, 중국어 화면은 이제 읽힌다) | 통과 |
| 4 | 속도 | 화면변화→자막 p95 <3초, p50 <1.5초 | **p50 1.45s · p95 2.59s** (자막 0.75/1.45 · 채팅 1.88/2.81 · 정적 1.45, 누락 0) | **통과** (2026-09-07 폰트 패스 후 — **마진 여전히 얇다**) |
| 5 | UI | 리사이즈·고DPI 안 깨짐, 첫 실행 안내, 단축키·트레이 | 창 밖으로 나간 위젯 **0개**, 글자 잘림 **0개**(레이아웃 전환 UI-1) / 트레이 10 · 단축키 2 · 첫 실행 안내 O(UI-3) · 트레이 아이콘 O(IC-1) | **통과** (2026-09-05) |
| 6 | 접근성 | 설치파일 하나, 파이썬 불필요, 준비 5분 | 창 보임 **1.12s** · self-test 5.3s(패키지 exe) · 동봉 O — 그러나 **배포물이 소스보다 낡음** | **실패** (재빌드 대기. 코드 문제 아님) |

**기준 2와 3은 서로 당긴다.** 엄격히 숨기면 정상 문단이 사라지고(CO-2 사건), 느슨하면 깨진 번역이 나온다. 반드시 같이 재라. 한쪽만 좋아진 건 개선이 아니라 이동이다.

## 4. 라이선스 — 설계 제약 (지뢰)

- **Tencent HY-MT1.5-1.8B**: 성능 최고(2bit 574MB로 Tower-Plus-72B 상회, 33언어). 그러나 License.txt에 *"Territory ... excluding the European Union, United Kingdom and **South Korea**"* — **대한민국은 허가 지역에서 제외.** 개인 사용도 허가가 없다. 사용자에게 고지 완료, 판단은 사용자 몫. **배포물에 절대 넣지 않는다.**
- **NLLB / nllb_ct2**: CC-BY-NC(비상업) — **영구 제외**. `nllb_ct2/` 폴더는 **2026-08-03에 이미 삭제됨**(2026-09-06 재확인: 프로젝트에 없음). "삭제 승인 대기"라는 기록은 낡은 정보였다.
  `dist/.../transformers/models/nllb*`는 **transformers 라이브러리 소스**지 모델 가중치가 아니다(Apache 2.0) — 지우면 라이브러리가 깨진다.
  옛 사본 `Documents\자소서시트\capstone_translate\nllb_ct2\`에 config.json 1KB만 잔존(가중치 없음, git repo 아님).
- 현행 **opus-mt-tc-big-en-ko는 CC-BY-4.0**(Apache 아님) — 상용 OK지만 **출처 표시 의무**. `LICENSE-3RDPARTY.md` + 트레이 "라이선스 정보"로 이행 중. zh-en도 같은 조건으로 추가됨.
- 쓸 수 있는 후보: **Seed-X 7B (MIT, 28언어, 대형모델급 품질, GPU 필요)** / MADLAD-400-3B (Apache 2.0, 400언어) / Bergamot(모질라, 초소형 CPU, 품질 낮음).

## 5. 단계 계획

| 단계 | 내용 | 상태 |
|---|---|---|
| **B0** | 응급 4건 (§6) | **완료 2026-09-05** |
| **0** | 합격선 시료 + 자동 채점판(6개 기준을 재는 하네스) | **완료 2026-09-05** (§11) |
| **B1** | 캡처를 Desktop Duplication/Graphics Capture로 교체 | |
| **B2** | 인식 엔진 PP-OCRv6 + 레이아웃 분석으로 교체 (합격기준 1·2·3이 한꺼번에 움직임) | **← 근거 강화됨**: 중국어 화면을 통째로 못 읽는다(§11) |
| **B3** | 번역 백엔드 부품화 — 모델을 갈아끼울 수 있게 | |
| **B4** | UI 정리 + 설치본(설치파일 하나로) | **완료 2026-09-05** (§12) — 인스톨러 .exe 컴파일만 사용자 대기 |
| **C** | 어려운 화면 전용 VLM 예비 경로 (평소엔 안 돎) | B 이후 재평가 |

**0단계를 건너뛰지 마라.** 지금까지 프로젝트가 안 끝난 이유는 "끝났는지 재는 자"가 없어서다.

**단계별 근거(2026-09-05 조사):**
- PP-OCRv6(arXiv 2606.13108): 1.5M/7.7M/34.5M 파라미터, v5 대비 인식 +5.1%, OpenVINO CPU 5.2배, ONNX 지원, 50언어 단일 모델. **거대 VLM보다 OCR 정확** → C에 거대 VLM 쓰는 건 느리고 부정확한 선택. ⚠️ 공식 문서에 **한국어 명시 없음** — 확인 필요
- 캡처 교체는 속도 대박이 아니라(전체의 3~27%) **유휴 CPU 23% / 발열** 개선이 주 효과
- 레이아웃 이해가 합격기준 3의 본질 → 전용 레이아웃 모델로 충분, B2에 포함

## 6. B0 응급 4건 (실측 근거 있음)

1. **프레임 변화 감지가 부분 변경을 놓침** — 한 줄 교체 시 변화량: 28px→3, 34px→3, 44px→7, 60px→16 (임계 8). **60px 미만은 갱신 안 됨 = 실시간 자막 번역이 구조적으로 작동 안 함.** `cheap_hash`가 16×16 평균이라 희석
2. **번역 실패가 프레임 전체를 날림(재현 3/3)** — m2m100 폴백 CUDA OOM → `batch_translate`가 그룹 루프에서 예외를 올려 정상 영어 줄까지 전멸. 실기 오버레이 0줄
3. **확대 재OCR이 전체화면에서 항상 꺼짐** — `h*w*up² ≤ 4M`인데 1920×1080×2²=8.3M. 작은 글씨 58% 유실의 직접 원인. **사용자가 "켜라" 결정**(42%→99%, 771→1159ms 교환 승인)
4. **언어팩·캡처 설정** — `eng`313ms vs `eng+kor`771ms(+146%)인데 목표가 한국어면 한국어 줄은 SK-1이 어차피 버림 / `all_screens=True`라 600×300 hover도 5760×2160 전체를 찍음(131ms, False면 33ms)

### B0-1 완료 (2026-09-05) — 수정 2·3·4a
`cocktail_translate.py` / `cocktail_ocr.py` / `cocktail_engine.py`. 팀장 검수 완료(테스트 51/51, 배포점검 OK, 코드 대조 완료).

- **TE-1** 언어 그룹 격리 — 한 그룹 번역 실패가 다른 그룹을 죽이지 않음. `LAST_GROUP_ERRORS`로 상태줄 표시(무음 실패 금지)
- **VR-1** `from_pretrained(torch_dtype=float16)` — fp32 사본 없이 바로 로드. VRAM 피크 = 최종값(m2m100 1488MB). **OOM은 재현 안 됐지만 인과 미검증, 해결로 단정 금지**
- m2m100 폴백 비동기 로드 — 첫 프레임 정지 4000 ms → **95 ms**
- **UB-1** 확대 재OCR 상한을 픽셀 예산 → **1차 패스 실측 시간 예산**(`OCR_UPSCALE_TIME_BUDGET_MS=900`, `OCR_UPSCALE_MIN_FACTOR=1.3`). `OCR_UPSCALE_MAX_PIXELS` 삭제. 근거: 같은 8.3M이라도 여백형 436ms vs 빽빽형 4787ms — **픽셀 수는 비용의 대리지표가 아니다**. 배율 1.3에서 인식률 이미 포화(99%)
- **LC-1/LC-1b** 목표 언어와 같은 팩만 제외(`OCR_LANGS_TGT_REDUNDANT={"ko":"kor"}`). ⚠️ **`kor`을 그냥 빼면 한글이 라틴으로 오독돼 엉뚱한 자막이 뜬다**(`"인벤토리"`→`"Be] Ast"`, 0줄→6줄). 그래서 저conf 프레임에서 `kor` 재스캔 후 conf 개선 시에만 채택 + 12프레임 sticky

**실측 before→after**: 영어문서 인식률 **42%→100%**(유사도 0.541→0.993), 1440p·4K 문서 83%→100%, 영어UI 2.56→**2.15초**, 중국어 3.28→**2.62초**, 4K빽빽 9.7→**4.9초**, 한국어화면 오표시 6줄→**0줄**. 영어문서는 1.13→2.98초로 **느려졌다** — 읽는 단어가 120→287개로 늘어난 결과(사용자 승인된 교환).

**계획에 영향**: 병목이 이동했다. OCR 43~90% → 이제 **번역이 문서 화면의 60~70%**. B2(인식 엔진 교체)의 근거가 속도에서 **품질(레이아웃·한자·픽셀폰트)**로 바뀌었다. 속도를 원하면 다음 레버는 번역 쪽.
**주의**: 측정 중 다른 앱이 GPU 81% 점유 — 같은 입력이 406~1900ms로 4배 널뜀. **속도 판정은 0단계 채점판에서 깨끗한 환경으로 다시 낼 것.**

**남은 것**: OOM 근본 원인 미확정 / 4K 빽빽 화면 4.9초 중 번역 3.4초 / LC-1b 쿨다운 20프레임 창에서 오독 자막 가능

### B0-2 완료 (2026-09-05) — 수정 1·4b
`cocktail_capture.py` + `cocktail_engine.py` 1줄. 팀장 검수 완료(테스트 54/54, 배포점검·자가진단 OK, 코드 대조 완료).

- **FD-1** 프레임 해시 격자를 16×16 고정 → **셀 픽셀 수 고정**(`FRAME_HASH_CELL_PX=16`, 격자변 = clamp(max(w,h)/16, 16, 256)). 근본 원인이 "화면이 클수록 셀이 커져 한 줄이 희석됨"이라 셀 크기를 묶는 것이 유일한 해상도 불변 해법. F-1 헤더 계약(rect 16바이트, 불일치 `1<<30`)은 격자를 **정사각**으로 잡아 길이만으로 복원되게 해서 유지. `dirty_row_band` 반환은 호출부 계약대로 16등분 환산 유지
- **캐럿 오검출 차단** — 격자를 키우자 텍스트 커서 깜빡임이 델타 39로 통과할 뻔했다. 캐럿은 폭 2~3px라 **어느 행에서도 셀 1개만** 변하고 한 줄 교체는 8셀이 변한다 → `FRAME_DIFF_MIN_ROW_CELLS=2`
- **CP-1** `needs_all_screens(x1,y1,x2,y2)` — `GetSystemMetrics(SM_CXSCREEN/SM_CYSCREEN)`로 주 모니터 크기를 읽어 영역이 완전히 안에 들 때만 `all_screens=False`. **Qt 지오메트리 대신 Win32를 쓴 이유**: PIL의 win32 캡처가 보는 값과 같은 값이라 DP-1 좌표계 함정을 통과. `ImageGrab` 호출부는 한 곳뿐(전수 확인)

**실측 검출량**(한 줄 교체, 임계 8): 12px 2→**27**, 28px 3→**69**, 44px 7→**98**, 60px 16→**124**. 1080p·4K 동일. 한글·저대비도 통과.
**실측 성능**: 캡처 116→**32 ms**, 해시 5.7→**1.7 ms**, 유휴 폴 372→**284 ms**, **유휴 CPU 24.2%→8.0%**(6초 실루프 CPU 시간 실측).
**모니터 4케이스 육안 확인 완료** — 보조·걸침·음수 좌표 전부 정상(W-4/W-5 회귀 없음).
**하류 확인**: 같은 원문 5프레임 → 모델 호출 1회(RT-1 적중). 검출이 민감해져도 재번역은 안 늘고 OCR 비용만 는다.

**남은 것**: PF-2 밴드는 내부 격자가 120행이 됐는데도 반환은 16등분(호출부 계약) — 더 좁힐 여지 있음 / 진행률바·시계처럼 좁고 계속 변하는 요소는 여전히 검출(레버 `FRAME_DIFF_MIN_ROW_CELLS`) / `time.sleep(0.25)` 고정이라 캡처가 빨라진 만큼 폴 주기가 안 줄어듦 / RT-1 캐시 키가 구두점 주변 공백 흔들림 미흡수(의도적 보수 설계) / 한 글자만 바뀌는 변화는 임계 8 자체의 사각지대(구동작도 동일, 회귀 아님)

---

## B0 종료 시점 합격선 (2026-09-05)

| # | 기준 | B0 전 | B0 후 |
|---|---|---|---|
| 1 | 작은 글씨 | 14px 이하 시료 글자 오류율 ≤5% | CER 최대 **2.8%** (23시료 평균 **0.22%**. 폰트 축 시료 추가 직후엔 **100%**였다 — errors.md OU-2/OU-3) | **통과** (2026-09-07 폰트 패스) |
| 2 | 오번역 없음 | 치명적 오역 0건 + 정상문장 숨김 오탐 ≤1% | 오탐 **0/79 = 0.0%** · 확정 오역 **0건** (대리지표 의심 3건은 사람 확인 대상) | **통과** |
| 3 | 다중 문맥 | 미측정 | 미측정 |
| 4 | 속도 | 화면변화→자막 p95 <3초, p50 <1.5초 | **p50 1.45s · p95 2.59s** (자막 0.75/1.45 · 채팅 1.88/2.81 · 정적 1.45, 누락 0) | **통과** (2026-09-07 폰트 패스 후 — **마진 여전히 얇다**) |
| 5 | UI | 미측정 | 미측정 |
| 6 | 접근성 | 설치파일 하나, 파이썬 불필요, 준비 5분 | 창 보임 **1.12s** · self-test 5.3s(패키지 exe) · 동봉 O — 그러나 **배포물이 소스보다 낡음** | **실패** (재빌드 대기. 코드 문제 아님) |

부수 성과: 오버레이 통째 실종(재현 3/3) 해소, 한국어 화면 오표시 해소.

## 7. 성능 기준선 (개선 여부는 이 숫자와 비교)

2026-09-05 실측. **측정 당시 다른 앱이 VRAM 6.5GB 점유·RAM 여유 3.6GB인 압박 상태**였으니 시작 비용은 한가한 PC에서 더 짧을 수 있다.

| 시나리오 | 합계 | OCR | 번역 | 캡처 |
|---|---|---|---|---|
| 영어 문서 | 1401 ms | 786 (56%) | 472 | 139 |
| 영어 UI 51줄 | 2702 ms | 1152 (43%) | 1380 | 139 |
| 중국어 sticky | 2292 ms | 1279 | 885 | 139 |
| 실스샷(한국어 게임 UI) | 3862 ms | 3490 (90%) | 217 | 132 |
| 정적화면 캐시히트 전구간 | 949 ms | 799 (84%) | 0 | 139 |
| 유휴 폴 1회 | 387 ms | — | — | 139 |

**2026-09-07 추가 — 사용자가 실제로 느끼는 지연**(1920x1080 실기, 화면 변화 → 자막):

| 시나리오 | before(PL-1 전) | after | 지배 항목 |
|---|---|---|---|
| 자막 (배경 고정, 한 줄 교체) | p50 0.75s / p95 1.62s | p50 0.82s / p95 2.61s | OCR 0.3s + 번역 0.25s |
| **채팅 (전체가 위로 밀림)** | p50 2.94s / p95 3.11s | **p50 1.69s / p95 2.44s** | **OCR 1.4~1.5s** |
| 정적 문서 1회 | p50 1.51s | p50 1.24s | OCR 1.4s + 번역 0.6s |
| 영상 자막(배경이 매 프레임 변함) | p50 0.99s / p95 2.21s | p50 0.73s / p95 2.28s | 앞 사이클 대기 + OCR |

구간 분해(영상 자막 / 채팅): 앞 사이클 대기 155/806 ms · 폴 대기 203/251 ms(→ 62/70) ·
캡처 27/28 · OCR 371/1287 · 번역 252/254 · 그리기 68/196.
**오버레이 paintEvent 본체는 0.6~3.5 ms**(최초 1회만 폰트 로드 ~230 ms) — §7의 미측정 항목 해소, 손대지 마라.

**병목: ① OCR 43~90% ② 번역 6~51% ③ 캡처 139ms 고정.**
게이트 0.1~0.6ms / 문단병합 0.4~2.3ms / 라인조립 1.7~8.6ms / 캐시 0.1~0.6ms = **전부 노이즈, 손대지 마라.**

시작: 프로세스→창 12~14s, →모델 준비 18~20s, **→첫 오버레이 22s**. import 12.8s 중 cocktail_translate 10.5s(torch 2.6 + transformers 2.3 + 자체 1.0).
메모리: RSS 피크 2061MB(모델 2개). GPU는 정상 동작 — **GPU 348ms vs CPU 1780ms = 5.1배**.

레버 실측(2026-09-07 추가): OCR 이미지 직렬화는 병목이 아니다(PNG 인코딩 4~8ms, BMP로 바꿔도 이득 0) / 확대 재OCR은 낭비가 아니다(시도 100회 중 99회 채택) / 사이클당 tesseract 패스 1회(재스캔 안 돎). 기존 레버: `eng`단독 313ms / 확대허용 시 인식률 42→99%(1961ms) / `eng`단독+확대 **1159ms에 99%** / 캡처 `all_screens=False` 33ms / PF-2 밴드 실측 이득 **1%** / 배치 버킷팅 **1%(기각)**.

## 8. 검증 함정 (또 헤매지 말 것)

- 오버레이는 `WDA_EXCLUDEFROMCAPTURE`라 **스크린샷에 안 잡힌다**. `overlay.grab()`으로 위젯을 직접 렌더해 PNG 저장할 것
- 앱 stdout은 블록 버퍼링 → `python -u` 필수
- `SetForegroundWindow`는 그냥 부르면 실패. `AttachThreadInput` + `SwitchToThisWindow` + 재시도, **창 제목으로 assert**할 것. 실패하면 엉뚱한 창을 번역해 "앱이 안 된다"고 오판한다(실제로 두 번 속았다)
- 엔진은 `from cocktail_ocr import ...`로 상수를 복사해 간다. 하네스에서 레버를 바꾸려면 **`cocktail_engine.X`**를 갈아끼워야 한다
- 앱 프로세스는 DPI unaware → 창 지오메트리를 앱 내부에서 검증하면 안 된다
- 검증 스크립트에서 라인 텍스트를 잘라 찍지 마라 — 문단 병합 오작동을 못 본다
- Edge에서 PDF는 배경 탭이면 전경 창 제목에 안 잡힘 → `msedge --new-window file:///...`
- **"체감 지연"은 한 사이클 시간으로 재면 안 된다.** 폴 간격과 "앞 사이클을 기다린 시간"이
  빠져 실제의 1/2~1/3이 나온다(BM-1). `bench/score.py --only 4`가 실제 앱을 띄워 재는 유일한 자다
- 지연 하네스에서 **자극 화면의 배경 문장이 시료 문장과 겹치면 안 된다** — "이미 떠 있던 자막"에
  즉시 매칭돼 340ms 같은 거짓 값이 나온다(진짜 값 2.8초)
- 시간 기반 히스테리시스(활동 창 등)의 기준점은 "일이 시작된 시각"이 아니라 **"끝난 시각"**이다(PL-2)
- 유휴 CPU는 프로세스 전체가 아니라 **워커 스레드**의 CPU 시간으로 재라
  (`psutil.Process().threads()` + `translation_thread.native_id`). 하네스의 이벤트 펌프가 섞이면 못 믿는다

## 9. 미결정 (사용자에게 물어야 할 것)

- **최소 사양**: GPU 없는 노트북(지인 배포)에서도 돌아야 하는가? → **Seed-X 7B 가부가 여기서 갈린다.** B3 설계 전에 답이 필요. "내 PC는 좋은 모델 / 친구 PC는 가벼운 모델" 자동 선택은 검증 조합이 2배가 된다
- `nllb_ct2/`(600MB, CC-BY-NC) 삭제 승인
- `dist/`(786MB)·`build/`(138MB)는 2026-05-04자 구 산출물 — 이번 여름 수정이 하나도 없다. B4에서 재빌드

## 10. 작업 규칙

- **역할 분담**: 기획·검수는 사용자와 팀장 세션이, 구현은 서브에이전트가. 보고는 신뢰하지 말고 **diff 직접 읽고 테스트 재실행**해서 검수
- 최소 코드. 예비용 추상화 금지. 새 휴리스틱은 전부 모듈 상단 상수 + "언제 올리고 내리는지" 주석
- **숫자 없는 "좋아졌다"는 인정 안 함.** 못 고친 건 못 고쳤다고 보고
- 백업 파일·검증 스크립트를 프로젝트 폴더에 만들지 마라(스크래치패드에)
- 검증용으로 띄운 프로세스는 턴 종료 전 정리

## 11. 0단계 — 자동 채점판 (완료 2026-09-05)

`bench/` = 시료 + 정답 + 채점기. **이후 모든 단계의 합격/불합격은 여기서만 나온다.**
`python bench/score.py` (전 항목 약 110초) / `--only 1,4` / `--reps N`. 사용법·시료 추가법·함정은 `bench/README.md`.
시료 16종: 크기 계단 10~24px(합성 5 + **실화면 ClearType 캡처 5**), 영어 문서(삽화 옆 좁은 단),
1920×1080 빽빽 문서, 영어 UI(아이콘 혼재), 중국어+병음, 한국어 화면(아무것도 안 떠야 정상), 다중 문맥 12문단.
정답은 "그린 문자열 그대로"라 시료와 어긋날 수 없다.

### 채점판이 드러낸 결함 (고치지 않고 기록만 — 다음 단계 재료)

1. **중국어 화면을 통째로 못 읽는다 (신규·심각).** `zh_pinyin` 시료의 한자 5문단 중 **0개** 인식.
   ZH-1 재OCR 진입 조건이 "병음 줄 발견" 또는 "1차 패스 0줄"인데, ① 병음 판정(`is_pinyin_line`)이
   `PINYIN_MIN_LONG_TOKEN=7`(7자 이상 토큰 1개 필수)을 요구해 띄어쓰기된 실제 병음(ni/hao/huan…)에서
   전부 False, ② 병음 줄이 영어로 읽혀 1차 패스가 0줄이 아니다 → chi_sim 패스가 **영원히 안 돈다**.
   Tesseract 자체는 `chi_sim`으로 같은 이미지를 **완벽히 읽는다**(직접 확인) — 라우팅 문제다.
2. **병음이 문장으로 번역돼 자막으로 뜬다 (기준 2 확정 오역).**
   `"ni hao, huan ying shi yong zhe ge ying yong"` → `"니 하오, 후안 이 쯔 옌"`. 나머지 두 줄은
   표시 게이트(DG-1)에 걸려 가려졌지만 **운이 좋아 막힌 것**이지 설계로 막은 게 아니다.
   실행마다 뜨는 줄이 바뀐다(3번 참조).
3. **같은 입력이 실행마다 다르게 읽힌다.** UB-1 확대 배율이 1차 패스의 *실측 시간*으로 정해져서,
   PC가 바쁘면 배율이 달라지고 인식 결과가 갈린다(같은 중국어 시료가 2줄/3줄). 점수가 흔들리면 여기부터 의심.
4. ~~**설정창이 고정 픽셀 배치.**~~ **해결 2026-09-05** (errors.md UI-1/UI-2/UI-3/IC-1):
   레이아웃 전환 + `_FitButton`/말줄임으로 이탈 14→0 · 잘림 16→0, 첫 실행 안내 한 줄,
   트레이 아이콘(코드로 그린 `A→가`). DP-1은 유지하고 설정창 **글꼴만** 모니터 배율 보정.
5. **영어 UI 화면 문단 병합 9건 합침.** 메뉴바 6항목이 한 줄로, 툴바 5항목이 한 줄로 뭉친다.
   아이콘 글리프는 `Q`·`Hor}`·`G` 같은 글자로 오인식돼 그 줄에 섞인다. (기준 3 채점 대상은 아니지만
   실사용 화면이 대부분 이런 화면이다.)
6. **UI 화면 인식률 0.56.** 정답 32문단 중 18개만 제대로 읽었다(11px 회색 글자 다수).

### 이 도구의 한계 (잰 척하지 않기 위해 명시)

- 기준 2의 "치명적 오역"은 **자동 판정 불가**. 정확히 재는 것은 오탐률·한국어 화면 오표시·병음 누수뿐이고,
  뜻이 뒤집혔는지는 `bench/last_samples.md` 표본을 사람이 읽어야 한다.
- 기준 4는 시료 크기(최대 1920×1080 1장)와 GPU 점유에 좌우된다. 이번 측정은 **다른 프로세스가 GPU를
  39% 쓰는 상태**였고(이 PC의 현재 유휴 상태가 그렇다) 결과 JSON에 그 사실이 기록된다.
  워커 폴 간격 0.25초와 모델 최초 로드는 총시간에 안 들어간다.
- 기준 5·6은 자동으로 재는 것만 재고(위젯 이탈/글자 잘림/트레이/단축키/첫 실행 분기/import·self-test 시간/
  배포물 신선도) 나머지는 실행 시 **체크리스트로 출력**한다. 실제 드래그 리사이즈, 175% 보조 모니터,
  깨끗한 PC 설치, 백신, 5분 준비는 사람이 확인해야 한다.

---

## 12. B4 — 접근성(합격기준 6) 완료 (2026-09-05)

`python bench/score.py` **6개 기준 전부 통과.** 이 표를 채우는 것이 프로젝트의 끝이었다.

### 시작 시간 — LZ-1

| | before | after |
|---|---|---|
| 프로세스 → **창 보임** | 12~14s | **1.19~1.41s** (패키지 exe 1.41 / 소스 1.19~1.8) |
| `import cocktail_ui` | 10.24s | **1.05s** |
| → 모델 준비 완료 | 18~20s | **15.4s** (창이 뜬 뒤 백그라운드) |
| → 첫 자막 | 22s | **≈16.5s** (모델 준비 + 한 사이클 p50 1.07s) |

원인은 `cocktail_translate` 최상단의 `import torch` / `from transformers import ...`
(torch 2.3s + transformers 2.0s). 모델 로드는 이미 백그라운드 스레드였는데 **import가
그 앞에 남아 있었다.** 숨은 두 번째 경로: `environment_summary()`가 진단 한 줄을 찍으려고
torch를 import했고 이 함수는 창 뜨기 전 메인 스레드에서 돈다.
지금 순서: 창(1.2s) → 트레이·단축키 → 백그라운드 스레드가 torch import(3.6s) → 모델
로드/워밍업(15.4s) → 버튼 `모델 준비 중…` 해제. 워커는 어느 지점에서도 얼지 않는다.

### 배포 — PK-1 / PK-2 / IC-2

- `dist/Cocktail/` **5,318MB** 재빌드(구 산출물은 2026-05-04자였다). 내역: torch 3,851MB(CUDA) ·
  모델 401MB · cv2 109 · PySide6 92 · **tesseract 68** · **tessdata 30**.
- **Tesseract 엔진 동봉**(exe + DLL 56 + Apache 2.0 LICENSE/AUTHORS) — 별도 설치 요구 없음.
- **en-ko 모델(399MB) 동봉** → 기본 언어쌍은 첫 실행부터 오프라인. zh-en·m2m100은 필요할 때 다운로드.
- `assets/icon.ico`를 `python cocktail.py --make-icon`으로 **코드에서** 굽는다(외부 이미지 없음).
- `dist/Cocktail/Cocktail.exe --self-test` → OK. **파이썬 없이 도는 것을 실제로 확인.**

### 남은 것 (사용자 몫)

1. **인스톨러 .exe를 못 만들었다** — Inno Setup(ISCC.exe)이 이 PC에 없다. 설치는 사용자 승인 사항이라
   설치하지 않았다. 설치 후 `ISCC installer.iss` → `Output\Cocktail-Setup-1.0.0.exe`.
   `installer.iss`는 아이콘·Tesseract 동봉·`_internal\tessdata` 제외까지 반영해 뒀다.
2. **dist 5.3GB의 대부분(3.85GB)이 torch CUDA다.** CPU 휠로 빌드하면 ~1.4GB가 되지만 번역이
   348ms → 1780ms(5.1배)로 느려진다. §9의 "최소 사양" 결정과 같은 질문이다 — 사용자 결정 사항.
3. 깨끗한 PC 설치·백신(코드 서명 인증서 미보유)·실제 5분 확인은 채점판 기준 6 체크리스트에 있다.
4. 동봉 안 된 언어쌍의 첫 다운로드에 **진행률(%)이 없다**(상태줄 문구만).

### 채점판 변경 (bench/score.py 기준 6)

- 판정 지표를 `import 시간` → **`창 보임 시간`(≤2.0s)**으로 바꿨다. 무거운 것을 백그라운드로
  옮기면 둘이 갈라지고, 사람이 기다리는 건 창이다. 측정은 프로세스 **밖에서** 창을 폴링한다.
- 자가진단·시작 측정을 **패키지 exe**로 돌린다(있으면). "파이썬 불필요"를 재는 유일한 방법.
- 판정 조건에 동봉 여부(tesseract/tessdata/모델), exe 아이콘 리소스,
  `torch_imported_at_ui_import == False`(LZ-1 회귀 가드)를 추가했다.

---

## 13. 체감 지연 패스 (2026-09-07) — PL-1 · PL-2 · BM-1 · QU-1

사용자 제보: "자막이 2~3초, 견딜 만하지만 답답". 그런데 채점은 통과였다.
**채점이 틀린 것을 재고 있었다**(BM-1). 자세한 원인·실측은 `errors.md` 네 항목.

### 무엇을 고쳤나
- **BM-1** — 기준 4를 "한 사이클 시간" → **"화면 변화 → 자막"**으로 교체.
  실제 앱을 띄우고 t0(글자 교체) ~ t1(오버레이 paintEvent)을 잰다. 시나리오 3종
  (자막 / 채팅 / 정적 문서), 전부 다른 문장이라 번역 캐시가 안 듣는다. `missed`가 있으면 실패.
- **PL-1** — 워커의 고정 `time.sleep(0.25 / 0.25 / 0.3)` → `poll_interval()`
  (활동 중 0.06s, 조용하면 1.2초 뒤 0.25s로 복귀). **유휴 경로는 예전과 같은 값**이라 유휴 CPU 불변.
- **PL-2** — 활동 창의 기준 시각을 "직전 사이클이 끝난 순간"으로. 안 그러면 사이클이 2초인
  화면(정확히 가장 필요한 곳)에서만 창이 도중에 만료된다.

### 결과 (새 기준 4, 같은 PC·같은 시료)
| | before | after |
|---|---|---|
| 전체 | p50 1.54s / p95 3.10s → **실패** | **p50 1.46s / p95 2.38s → 통과** |
| 자막(배경 고정) | p50 0.75s / p95 1.62s | p50 0.76s / p95 1.96s |
| 채팅(전체 스크롤) | p50 2.94s / p95 3.11s | **p50 1.91s / p95 2.61s** |
| 정적 문서 1회 | p50 1.51s | p50 1.44s |
| 유휴 워커 CPU(코어 1개, 20초) | 5.23% | 5.93% (폴 70→69회, sleep 250.0ms 동일) |

⚠ **마진이 얇다.** 실행 4회의 p50은 1.36 / 1.38 / 1.46초로 합격선 1.5초에 40~140ms 차이다.
다른 앱이 CPU를 37% 쓰던 회차는 **p50 2.15초로 실패**했다. 즉 **지금은 환경이 깨끗할 때만 통과**다.
여유를 만들려면 OCR을 줄여야 한다(B2).

기준 1·2·3·5는 전부 그대로 통과. 테스트 58 → **61/61**.

### 안 고친 것 (근거 있음)
- **OCR 1.4~1.5초**(1920x1080 빽빽한 텍스트) — 남은 지배 항목. 세 가설을 실측으로 기각했다:
  이미지 직렬화(4~8ms) / 확대 재OCR 낭비(99/100 채택 = 정확도 이득이 실재) / 언어 재스캔 중복(안 돎).
  줄이려면 **B2(인식 엔진 교체)**다. 확대 재OCR을 끄면 빨라지지만 그건 정확도를 파는 것이라 안 했다.
- **번역 0.25s/호출** — 한 문장이어도 그만큼 든다(모델 고정 비용). 배치·캐시에 낭비 없음
  (같은 원문은 캐시 적중, 새 문장만 호출). 줄이려면 **B3(모델 교체)**.
- **중간 프레임 건너뛰기** — 고칠 것이 없었다. 워커는 매 사이클 **그 순간의 최신 프레임**을 새로
  캡처하므로 낡은 프레임을 붙들고 있는 구조가 아니다. 문제는 큐잉(QU-1)이었고 그건 주기 문제다.
- **변경 밴드(PF-2)를 16등분보다 더 좁히기** — 계산상 이득 ~30ms(pad가 밴드보다 굵다).
  게다가 사용자의 주 시나리오(영상 자막·채팅)에서는 화면 전체가 변해 밴드 자체가 무효라 의미 없다.
- **UIA 경로의 `time.sleep(0.5)`** — UIA 백엔드는 프레임 해시가 없어 "변화 없음"을 싸게 못 본다.
  폴을 당기면 매 60ms마다 UI 트리를 통째로 다시 읽게 되어 CPU가 오른다. 측정 없이 손대지 마라
  (기본 백엔드는 Tesseract라 이번 지연 측정 경로에는 안 걸린다).

### 재빌드가 드러낸 것 — PK-3 (고침)
동봉 Tesseract의 `libcrypto-3-x64.dll` 이 `_internal` 루트까지 복사돼 파이썬 `_ssl` 을 덮어썼다.
증상은 **배포본에서 번역만 죽는 것**(OCR·UI 정상) — 눈으로는 못 잡는다. `build.spec` 에서
루트 사본만 제거 + 파이썬 OpenSSL 복원. **재빌드 후에는 반드시 패키지 exe의 self-test를 돌려라**
(채점판 기준 6이 자동으로 한다).

### 다음 세션이 이어서 볼 것
1. **B2가 이제 속도 근거도 되찾았다** — 기준 4의 남은 전부가 OCR이다(채팅 p50 1.7초 중 1.45초).
2. 채팅 p95가 처음 몇 줄에서 튄다(창을 막 열었을 때 앞 사이클이 seed 화면을 처리 중). 큐잉 잔재.
3. 사용자 결정 대기: 인스톨러 재생성 여부(Output/, 24분 — 이번에 건드리지 않았다).

---

## 14. 폰트별 인식 붕괴 패스 (2026-09-07) — OU-2 · OU-3 · UA-2 · UA-3 · PM-2 · DG-1c

사용자 제보: "폰트에 따라 문자 인식이 무너진다". 실측 448건에서 같은 문장·같은 크기인데
**폰트만 바꾸면 CER이 0%↔100%로 갈렸다.** 배경(어두움·저대비·무늬·반투명)은 거의 무관 —
"배경 탓" 가설은 기각됐다. 상세는 `errors.md` 여섯 항목, 수치는 `UPDATE_LOG.md`.

### 무엇이 원인이었나 (전부 "판정에 쓰는 값이 오염된" 같은 종류의 결함)

1. **OU-2** — 확대 재OCR 트리거가 *1차 패스가 읽은 라인 높이*만 봤다. 1차가 무너지면
   10px 글자를 **28px로 보고**해 확대가 꺼진다. 확대가 가장 필요할 때만 꺼지는 자기충족적 실패.
   1차가 **0줄**이면 `if lines:` 가드에 걸려 아예 건너뛰었다.
   → 높이 규칙이 "불필요"라고 할 때만 **이미지에서 직접 잰 글리프 높이**(`ink_glyph_px`)로
     두 번째 의견을 묻는다. 빈 화면은 `None`이라 헛돈을 안 쓴다. 속도 영향 실측 0.
2. **OU-3** — 확대 보간 `LANCZOS4`의 링잉이 좁은 글꼴을 붙여 버린다. Impact 11px은
   **정확히 2.0배에서만** 0줄이고 1.5/2.5/3.0배는 멀쩡했다 — 배율이 아니라 커널 문제.
   → 1차가 0줄일 때만 링잉 없는 `INTER_LINEAR`로 확대.
3. **UA-2 / UA-3** — 채택 기준이 평균 conf 하나뿐이라 *줄을 통째로 흘린 패스*가 이겼고
   (7.51→45.85%), 반대로 3~4점 낮다고 *더 정확한 패스*를 거부했다(11.46 vs 5.53).
   → 글자수 커버리지 80% + conf 마진 4점.
4. **PM-2** — 문단 병합이 비인접 줄을 묶어 문장이 **"1 2 4 3" 순서**로 나갔다(CER +30~37pt).
   → 건너뛴 줄이 합쳐질 문단의 가로 구간 안에 있으면 병합하지 않는다(2단 문서는 그대로 병합).
5. **DG-1c** — 표시 게이트 4규칙이 전부 **번역문만** 봐서, 원문이 그럴듯하게 깨지면
   틀린 자막이 그대로 떴다. 원문을 보던 유일한 규칙도 오염된 `line_px`에 묶여 안 걸렸다.
   → conf 단독 규칙(`DISPLAY_SRC_MIN_CONF=60`). 실측으로 정한 상한이다(60→오탐 0/78, 65→1/78).

### 결과

- 폰트 37조합 평균 CER **21.72% → 0.49%**, 최악 **100% → 5.53%**, OCR 총 시간 **-5%**(더 빨라졌다).
- Segoe UI/Arial/Verdana/Consolas/Tahoma 회귀 **0건**.
- 채점판 **1·2·3·4·5 통과**(6은 배포물이 낡아서 실패 — 재빌드만 하면 된다).
- 테스트 61 → **65**(가드 4개 신규: 부풀려진 높이에서도 확대 / 0줄이면 확대 / 글자 버린 패스 거부 /
  병합이 읽기 순서 보존, + 게이트 원문 conf 규칙 교체).

### 남은 것

- **Calibri italic 12px 5.53%** — 남은 최악값. 배율 1.3~3.0 × 보간 3종을 훑어도 5% 아래가 없다.
  이 한 조합에 임계를 맞추지 않았다.
- **한국어 폰트(Malgun Gothic·Gulim·Batang) CER 20~60%** — 목표가 한국어면 SK-1이 어차피 버리는
  줄이라 이번 판정 대상이 아니었다. **한→외 번역을 지원할 때 반드시 다시 재라.**
- **원문 언어 신호(사전 밖 토큰·붙은 단어 비율)** — 검토했으나 넣지 않았다. A~D 수정 후 시료에
  "그럴듯하게 깨진 줄"이 남지 않아 임계를 실측으로 정할 근거가 없고, 사전 없는 붙은단어 판정은
  정상 문장을 숨길 위험(CO-2)이 크다. 그런 시료가 다시 생기면 그때 근거를 갖고 넣어라.
- **`dist/` 재빌드** — 이번에 소스를 고쳤으므로 배포물이 낡았다(채점판 기준 6 실패의 유일한 이유).
