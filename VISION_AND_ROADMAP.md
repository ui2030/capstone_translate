# Cocktail — 비전과 로드맵

> **컴퓨터를 모국어로 보는 가장 짧은 길.**
> 인식·번역 전 과정 로컬 처리. 화면 내용과 번역 결과는 외부로 나가지 않습니다.
> (최초 1회 모델 다운로드 + Tesseract 별도 설치는 필요)

---

## 1. 비전

**"내가 사용하는 언어로만 볼 수 있는 세상"** — OS UI 전반을 사용자 모국어로 자동 번역.

영역 박스를 그어 한 곳을 번역하는 도구에서 출발했지만,
궁극 목표는 **시스템 전반의 외국어를 모국어로 자연스럽게 보이게 하는 환경**입니다.
시작 메뉴, 설정 앱, 알림 토스트, 메뉴 항목, 동영상 자막, 게임 NPC 대사 — 어디든.

---

## 2. 사회적 가치

| 영역 | 의미 |
|---|---|
| **접근성** | 영어 진입장벽으로 컴퓨터를 어려워하는 노년층/외국 노동자/학생에게 평등한 접근 |
| **인지 부하 감소** | "영어 → 한국어" 번역에 쓰이는 brain cycle 절약 |
| **국제화 보완** | 많은 앱이 한국어 패치를 따로 제공하지 않음. OS 레벨에서 메우는 보완재 |
| **장애인 보조** | 시각 장애인 스크린 리더(NVDA, JAWS)가 영어 환경 위주인 한계의 대안 |

---

## 3. 시장 분석

| 영역 | 상태 |
|---|---|
| **모바일 OS 통합 번역** | Apple Live Text, Google Lens 가 점유 — 포화 |
| **PC OS 통합 번역** | **메이저 제품 없음 — 빈 자리** |
| 클립보드/선택 텍스트 | DeepL (Ctrl+C+C), Mate Translate — OCR 없음 |
| PC 화면 OCR | PowerToys Text Extractor (번역 X), QTranslate (2018 이후 멈춤), Capture2Text |
| 게임/비주얼노벨 특화 | LunaTranslator, Textractor — 일반 데스크톱에 부적합 |

→ **PC 일반 사용자용 "백그라운드 자동 번역"은 시장 공백**.

---

## 4. 차별 포인트 (마케팅 1페이지 핵심)

1. **로컬 추론** — 화면 내용의 클라우드 전송 없음. 비밀번호/대화/의료기록 안전
   (외부 통신은 최초 1회 모델 다운로드뿐)
2. **상용 라이선스 호환** — PySide6 LGPL + opus-mt Apache 2.0 + m2m100 MIT (NLLB·PyQt5 GPL 회피)
3. **자기 캡처 피드백 차단** — `WDA_EXCLUDEFROMCAPTURE`. 다른 OCR 오버레이 도구는 이 함정을 안 막음 (errors.md B-10)
4. **한국어 사용자 친화** — 메뉴/UI/단축키 한국어 우선 설계
5. **UIA + OCR 하이브리드** (계획) — UI Automation으로 정확하게, 못 읽는 영역만 OCR 폴백

---

## 5. 보안/윤리 원칙 (절대 위반 금지)

| 원칙 | 구현 |
|---|---|
| **로컬 추론** | 화면/번역 데이터의 클라우드 전송 금지. 모델은 사용자 디스크에서 추론. 네트워크는 최초 1회 모델 다운로드에만 사용 |
| **번역 데이터 비저장** | 메모리 캐시만. 디스크 캐시는 hash key 기반 (평문 저장 X) |
| **민감 영역 자동 OFF** | 패스워드 입력 창/결제 페이지/은행 앱 감지 시 자동 일시 중지 |
| **오픈 코어** | 핵심 로직 공개 → 사용자 신뢰 확보 (수익은 프리미엄 옵션으로) |
| **즉시 토글** | 글로벌 단축키로 언제든 끔 (Ctrl+Shift+H 외) |

---

## 6. 기술 접근 — UIA + OCR 하이브리드

### A. UI Automation (UIA) — 1차
- Windows 내장 접근성 API. 모든 표준 UI 요소(버튼/라벨/메뉴/툴팁)의 **텍스트를 OCR 없이 직접 읽기**.
- 시작 메뉴, 설정, Office, 브라우저, Explorer, 작업 관리자 → 100% 정확.
- 스크린 리더(NVDA, JAWS)가 사용하는 동일 기술.

### B. OCR — 2차 폴백
- UIA가 노출하지 않는 영역(게임/Custom GDI/동영상 자막)만 처리.
- 현재 Cocktail의 Tesseract / PaddleOCRv5 그대로 사용.

### C. 번역 + 렌더링 — 현재 그대로
- `HybridTranslator` (opus-mt + m2m100).
- 원래 위치 위에 모국어 풍선 오버레이.

### 결과
| 시나리오 | 처리 |
|---|---|
| 시작 메뉴/설정/브라우저 | UIA로 즉시, 정확 |
| 게임 NPC 대사 | OCR 폴백 |
| 동영상 자막 | OCR 폴백 |
| 패스워드 입력 창 | 자동 OFF (보안 원칙 3) |

---

## 7. 단계별 로드맵

| Phase | 내용 | 상태 |
|---|---|---|
| **0** | 영역 박스 OCR + 한국어 오버레이 | ✅ 완료 (베타 가능) |
| **1A** | 시스템 트레이 + 글로벌 단축키 + 모드 콤보 (박스/활성 창/마우스 hover) | ✅ 완료 (T-1, T-2) |
| **1B** | 마우스 통과 토글 (`Ctrl+Shift+P`) | ✅ 완료 (W-1) |
| **1C** | OverlayWindow 분리 (전체 화면 + 항상 마우스 통과) | ✅ 완료 (W-2) |
| **1D** | 멀티 모니터 대응 (가상 데스크톱 전체) | ✅ 완료 (W-3) |
| **2/3** | UIA 어댑터 + 워커 통합 (활성 창 텍스트 직접 추출) | ✅ 1차 완료 (UIA-1) — 사용자 검증 대기 |
| **4** | DPAPI 영구 캐시 (학습형, hash key, 암호화) | ✅ 1차 완료 (D-1) — 주기적 save 다음 단계 |
| **5** | 민감 영역 자동 OFF (패스워드/결제/은행) | ✅ 1차 완료 (S-1) — UIA 모드 일관 적용 |
| **6** | 마켓 진입 자산 (LICENSE, 코드 서명 가이드, GitHub Actions, 자동 실행, 영어 README) | ✅ 1차 완료 (A-1) — 인증서/디자이너/베타테스터 추후 |

### Phase 1 작업 항목 (다음 코드 작업)
- `QSystemTrayIcon` + `QMenu` (시작/정지/모드/언어/종료)
- 메인 창 hide/show 토글 (트레이 더블클릭)
- 글로벌 단축키 (`keyboard` 패키지 또는 `RegisterHotKey` ctypes)
- 캡처 모드 콤보:
  - 영역 박스 (현재)
  - 활성 윈도우 (`GetForegroundWindow` + `GetWindowRect`)
  - 마우스 hover (커서 ± N px)
  - 전체 화면 (선택)
- Windows 시작 시 자동 실행 옵션 (레지스트리 `HKCU\...\Run` 또는 시작 폴더 .lnk)

### Phase 2 작업 항목 (PoC)
- `uia_explore.py` 스크립트 — 활성 창의 UIA 트리를 콘솔에 출력
- 어떤 앱이 어디까지 읽히는지 매트릭스화
- 본 코드 통합 결정은 PoC 결과 후

---

## 8. 한계 (박사 정직 보고)

- **Windows 우선** — UIA는 Windows 전용. macOS/Linux 이식은 AT-SPI/AX API로 별도 작성
- **일부 영역 OCR 폴백만 가능** — 게임, Custom GDI, 일부 Electron 앱
- **동적 UI** — 빠른 애니메이션/드롭다운은 따라잡기 어려움
- **고유명사 보존 휴리스틱 필요** — `AnySign4PC.exe`, `Anthropic, PBC` 같은 식별자는 번역 안 함
- **첫 출시는 Windows만** — 충분 (데스크톱 70%+ 시장)

---

## 9. 마케팅 카피 (1페이지 카피 초안)

> ## Cocktail
> ### 컴퓨터를 모국어로 보는 가장 짧은 길.
>
> 작업 관리자, 설정 메뉴, 알림 토스트, 동영상 자막 — 어디서든 자동으로 모국어가 떠오릅니다.
>
> **로컬 처리**. 모든 화면은 사용자 PC 안에서만 처리되며, 화면 내용이나 번역 결과가 외부 서버로 나가지 않습니다.
> (설치 후 최초 1회만 번역 모델을 내려받고, 그 뒤로는 인터넷 없이 동작합니다.)
> 비밀번호 입력 창에서는 자동으로 멈춥니다. 언제든 단축키 한 번으로 끌 수 있습니다.

---

## 10. 다음 액션

박사 권장 순서:
1. **Phase 1 코드 진행** — 트레이 + 글로벌 단축키 + 모드 다양화 (1주 내)
2. **Phase 2 PoC** — `uia_explore.py` 별도 실험. 실 데스크톱에서 검증
3. PoC 결과 좋으면 Phase 3 통합. 별도 회의 후 결정

진행 의사가 있을 때 박사에게 알려주세요.
